// Legacy stub. Base44 is gone; nothing reads from this any more.
// Kept only to avoid touching the few imports that may still reference it.
export const appParams = { appId: 'tempo-ai-hub', token: null, fromUrl: null, functionsVersion: null, appBaseUrl: null };
